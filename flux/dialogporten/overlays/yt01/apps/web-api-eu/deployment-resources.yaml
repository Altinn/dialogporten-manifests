apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-api-eu
spec:
  template:
    spec:
      containers:
        - name: web-api-eu
          resources:
            requests:
              cpu: "2"
              memory: 4Gi
            limits:
              cpu: "2"
              memory: 4Gi
