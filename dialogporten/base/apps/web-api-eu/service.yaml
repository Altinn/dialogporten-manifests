apiVersion: v1
kind: Service
metadata:
  name: web-api-eu
  labels:
    app.kubernetes.io/name: web-api-eu
    app.kubernetes.io/part-of: dialogporten
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web-api-eu
  ports:
    - name: http
      port: 80
      targetPort: 8080
