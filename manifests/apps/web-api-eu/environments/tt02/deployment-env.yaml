apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-api-eu
spec:
  template:
    spec:
      containers:
        - name: web-api-eu
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: staging
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "1"
