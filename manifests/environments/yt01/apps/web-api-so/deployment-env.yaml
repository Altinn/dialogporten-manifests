apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-api-so
spec:
  template:
    spec:
      containers:
        - name: web-api-so
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: yt01
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "1"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: web-api-so-runtime
  labels:
    app.kubernetes.io/instance: yt01
data:
  DIALOGPORTEN_ENVIRONMENT: yt01
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: web-api-so-secrets
  labels:
    app.kubernetes.io/instance: yt01
