apiVersion: batch/v1
kind: Job
metadata:
  name: web-api-migration-job
spec:
  template:
    spec:
      containers:
        - name: web-api-migration-job
          env:
            - name: DOTNET_ENVIRONMENT
              value: test
